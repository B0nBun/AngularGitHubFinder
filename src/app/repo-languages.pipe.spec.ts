import { RepoLanguagesPipe } from './repo-languages.pipe';

describe('RepoLanguagesPipe', () => {
  it('create an instance', () => {
    const pipe = new RepoLanguagesPipe();
    expect(pipe).toBeTruthy();
  });
});
